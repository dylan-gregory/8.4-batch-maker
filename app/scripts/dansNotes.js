 /**
  *  Adding "Active" class to highlight nav bar locations in React
  **/

  // function isActive(url){
  //   return Backbone.history.fragment == url;
  //
  // }
  //
  // function activeClass(url){
  //   return isActive(url) ? 'active' : '';
  // }
  //
  // <li className={activeClass('auth/')}></li>
  //
  // setup a nav item component
  //
  // <NavItem url="auth/" name="Login"/>
  //
  // it would also add the "sr-only"(current) span tag in too

  To pull out only items that were made by me, WHERE clause - under queries in the docs
  
